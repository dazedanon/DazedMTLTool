interface:
  display_name: "Game Walkthrough Builder"
  short_description: "Build detailed, source-traceable game guides"
  default_prompt: "Use $build-game-walkthrough to audit the game and publish all four verified views, with source-bound ordered instructions that preserve every actionable area transition, gate, encounter, acquisition, return, turn-in, and visible completion cue instead of reducing routes to summaries."
